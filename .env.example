# (Removed) Anthropic API Key was used for AI features which are no longer included

# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Set true when running `vercel dev` to test the local Vercel Supabase proxy.
VITE_USE_SUPABASE_PROXY=false

# Production builds use the Vercel /supabase rewrite automatically.
